{
  "main": "reptar-homepage",
  "scripts": {
    "deploy": "reptar build && gdd --branch master --src _site --remote origin"
  },
  "dependencies": {
    "bootstrap": "^3.3.5",
    "normalize.css": "^3.0.3",
    "reptar-excerpt": "^0.5.0",
    "reptar-html-minifier": "^0.5.0"
  },
  "devDependencies": {
    "@hswolff/git-directory-deploy": "^0.5.5"
  }
}
